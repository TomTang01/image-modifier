# Agent Notes

- Canonical project path: `E:\Image Modifier`.
- GitHub remote: `git@github.com:TomTang01/image-modifier.git`.
- This is a solo project in a newly created empty GitHub repository. Future chats should not spend extra effort rediscovering or reconnecting the remote unless the user explicitly asks.
- Streamlined Git handling is acceptable for this repo context, but still avoid destructive file or Git actions unless the user clearly requests them.
- Main app entrypoint: `app.py`.
- Run command: `streamlit run app.py`.
- The znelchar editor was extracted into the standalone project at `E:\Znelchar File Editor`.
